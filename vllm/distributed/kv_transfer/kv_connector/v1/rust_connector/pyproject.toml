[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "vllm-rust-kv-connector"
version = "0.1.0"
description = "Rust-based KV Connector for vLLM using PyO3"
requires-python = ">=3.10"

[tool.maturin]
# leave default, will use lib name from Cargo.toml
